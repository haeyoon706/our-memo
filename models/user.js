export class User {
  constructor(id, email, nickname, roomList, createdAt) {
    this.id = id;
    this.email = email;
    this.nickname = nickname;
    this.roomList = roomList;
    this.createdAt = createdAt;
  }
}
